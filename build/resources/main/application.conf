ktor {
    development = true
    deployment {
        port = 8080
        port = ${?PORT}
                watch = [ resources ,classes]
    }
    application {
        modules = [ com.example.ApplicationKt.module ]
    }
}
